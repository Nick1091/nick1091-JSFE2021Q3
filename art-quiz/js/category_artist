const playList = new CategoryFilling()
  
class CategoryFilling  {
  constructor(image, currentItem) {
    this.image = image;
    this.currentItem = currentItem;
  }
  getImage(
   imageCategory = document.querySelectorAll('.image_category');
   const image = new Image();
   image.src = `.assets/image/${currentItem}.jpg`;
   image.onload = () => {

   }
  )
}
export default playList;